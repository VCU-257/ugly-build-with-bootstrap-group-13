# Student Name: Hrishita Sehrawat

## 1. My Assigned Work
I was responsible for building the Home Page and global navigation system for our application. 

## 2. Bootstrap Implementation
I implemented the layout using default Bootstrap 5 components, focusing on structure rather than custom styling. 
* **Components Used:** *(e.g., Bootstrap Navbar, Cards, Form Controls, Tables)*
Navbar -> for top navigation across all pages
Container & Grid (container, row, col) -> for layout structure
Buttons (btn, btn-primary, btn-outline) -> for actions like navigation
Cards -> for sections like "Quick Start" and content blocks
Typography classes -> for headings and text hierarchy
Utility classes (mt, p, text-center, etc.) -> for spacing and alignment

## 3. Technical Challenges & Solutions
[cite_start]*Look back at Table 4: Technical Risks. Did you build any of those risky components? How did you solve the challenge? If you didn't face those specific risks, what was the hardest part of your assigned work?*
One of the main challenges was creating a consistent navigation system across multiple pages without conflicts when working in a shared repository. Another challenge was making sure all pages were properly linked and that clicking buttons (like "Create Character" or navigation tabs) led to the correct screens. 
To solve this, I reused the same navbar structure across all HTML files, used simple anchor links to simulate navigation between pages. 

## 4. AI / LLM Usage
*Did you use an AI tool to help write or debug your code?*
* **What I asked the AI:** *(e.g., "How do I make a sticky footer in Bootstrap 5?")*
* **How it helped & What I learned:** *(Explain the solution it provided and how you ensured you understood the code).*
Yes, I used ChatGPT to assist with development and debugging. I asked the AI how to structure a Bootstrap navbar across multiple pages, how to connect pages using HTML links, how to organize a multi-page layout using Bootstrap, and how to fix git branch issues.
The AI helped me understand how to properly structure HTML files and reuse components like navbars. I made sure to review and test all code myself to understand how each part works, especially how navigation between pages is handled. 

## 5. Live Site Link
*Provide the GitHub Pages link to the specific page(s) you built.*
* **Live URL:** [Insert Link Here]